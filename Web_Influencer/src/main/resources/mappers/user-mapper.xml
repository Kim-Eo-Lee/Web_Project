<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
                        
<mapper namespace="User">
	<select id="login" parameterType="com.ssafy.happyhouse.dto.MemberInfo" resultType="com.ssafy.happyhouse.dto.MemberInfo">
		SELECT userid, userpw, username, useraddress, userphone, userdong
		FROM ssafydb.housemember
		WHERE housemember.userid = #{userid}
		AND housemember.userpw = #{userpw}
	</select>
	<insert id="join" parameterType="com.ssafy.happyhouse.dto.MemberInfo">
		insert into ssafydb.housemember (userid, userpw, username, useraddress, userphone, userdong)
		values (#{userid}, #{userpw}, #{username}, #{useraddress}, #{userphone}, #{userdong})
	</insert>
	<select id="findpw" parameterType="com.ssafy.happyhouse.dto.MemberInfo" resultType="String">
		select userpw
		from ssafydb.housemember
		where userid = #{userid} and useraddress = #{useraddress}
	</select>
	
	<delete id="deluser" parameterType="com.ssafy.happyhouse.dto.MemberInfo">
		delete from ssafydb.housemember
		where userid = #{userid}
	</delete>
	
	<update id="modify" parameterType="com.ssafy.happyhouse.dto.MemberInfo">
		update housemember
		set userpw = #{userpw}, username = #{username},
		useraddress = #{useraddress}, userphone = #{userphone}, userdong = #{userdong}
		where userid = #{userid}
	</update>
</mapper>
